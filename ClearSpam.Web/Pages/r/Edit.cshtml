@page
@model ClearSpam.Web.Pages.r.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Rule</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Rule.Id" />
            <div class="form-group">
                <label asp-for="Rule.AccountId" class="control-label"></label>
                <select asp-for="Rule.AccountId" class="form-control" asp-items="ViewBag.AccountId"></select>
                <span asp-validation-for="Rule.AccountId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Rule.Field" class="control-label"></label>
                <input asp-for="Rule.Field" class="form-control" />
                <span asp-validation-for="Rule.Field" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Rule.Content" class="control-label"></label>
                <input asp-for="Rule.Content" class="form-control" />
                <span asp-validation-for="Rule.Content" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
